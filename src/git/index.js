import { BaseGenerator } from '../utils';

export default class GitGenerator extends BaseGenerator {
	configuring() {
		// TODO git initialization
	}
	writing() {
		// TODO package field for git(hub) repo
	}
	end() {
		// TODO git push
	}
}
