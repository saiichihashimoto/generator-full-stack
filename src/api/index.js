import { BaseGenerator } from '../utils';

export default class ApiGenerator extends BaseGenerator {
	configuring() {
		// TODO api writing
	}
	writing() {
		// TODO server bindings to api
		// TODO website bindings to api
	}
}
