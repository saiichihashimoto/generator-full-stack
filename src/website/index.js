import { BaseGenerator } from '../utils';

export default class WebsiteGenerator extends BaseGenerator {
	configuring() {
		// TODO postcss setup
		// TODO website writing
	}
	writing() {
		// TODO package script for css formatting
		// TODO package script for css linting
		// TODO server bindings for rendering on serving
		// TODO website bindings for rendering on build
	}
}
