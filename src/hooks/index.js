import { BaseGenerator } from '../utils';

export default class HooksGenerator extends BaseGenerator {
	writing() {
		// TODO package fields/scripts for commit convention hooks
		// TODO package fields/scripts for linting hooks
		// TODO package fields/scripts for formatting hooks
	}
}
